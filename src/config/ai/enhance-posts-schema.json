{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The original post ID to ensure mapping is correct."
      },
      "summary": {
        "type": "string",
        "description": "A concise summary of the post, around 100-150 words."
      },
      "keywords": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 3,
        "maxItems": 5,
        "description": "A list of 3-5 relevant keywords for the post."
      }
    },
    "required": [
      "id",
      "summary",
      "keywords"
    ],
    "additionalProperties": false
  }
}